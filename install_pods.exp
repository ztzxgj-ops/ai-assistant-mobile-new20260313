#!/usr/bin/expect -f
set timeout 600
set password "102330"

# 尝试强制安装 CocoaPods (使用系统 Ruby)
spawn sudo gem install cocoapods -v 1.12.1 --no-document
expect "Password:"
send "$password\r"
expect eof

# 验证是否安装成功
spawn pod --version
expect eof
