#!/usr/bin/expect -f
set timeout 20
set password "gyq3160GYQ3160"

spawn ssh root@47.109.148.176 "find /etc -name '*ai-assistant*' 2>/dev/null && echo '---' && find /etc/supervisor* -name '*.conf' 2>/dev/null | head -10"
expect "password:"
send "$password\r"
expect eof
